# دليل تشغيل المدونة

## أولاً — نشر الموقع (نصف ساعة، مرّة واحدة)

1. أنشئ حساباً على **github.com**. جرّب اسم المستخدم `marwanbalushi` أولاً، وإن كان مأخوذاً فـ`marwanbalushi80`.
2. اضغط **New repository**، وسمّه بالضبط: `اسم-المستخدم.github.io`
   (إن كان اسمك `marwanbalushi80` فالمستودع اسمه `marwanbalushi80.github.io`).
   اجعله **Public**، ولا تؤشّر على أي خيار إضافي.
3. حمّل برنامج **GitHub Desktop** من `desktop.github.com` وثبّته، ثم سجّل الدخول بحسابك.
   > **لماذا برنامج ولا نرفع من المتصفح؟** لأن الرفع من المتصفح محدود بمئة ملف في المرة،
   > وهذه المدونة تحوي ١٬٩٨٠ ملفاً، فستحتاج عشرين رفعة. والبرنامج يرفعها دفعة واحدة.
4. في البرنامج: **File ← Clone repository**، واختر مستودعك من القائمة، واحفظه في مكان تعرفه.
5. افتح المجلد الذي نُسخ، والصق فيه **محتويات** هذا المجلد كلها (لا المجلد نفسه).
6. ارجع إلى البرنامج، فسيظهر لك ٢٬٠٠٠ ملف جديد. اكتب في الخانة السفلية اليسرى أي كلمة
   (مثل `الأرشيف`)، واضغط **Commit to main**، ثم **Push origin** في الأعلى.
7. انتظر دقيقتين، ثم افتح `https://اسم-المستخدم.github.io`.

**بديل بلا برامج** إن تعذّر تثبيت البرنامج: ارفع من المتصفح على دفعات.
ابدأ بكل شيء عدا مجلد `_posts` (نحو ١٥ ملفاً)، فيعمل الموقع فوراً بصفحته الرئيسة.
ثم ارفع محتوى `_posts` على دفعات من مئة، بفتح المجلد داخل GitHub واختيار
**Add file ← Upload files** في كل مرة. مُرهِق لكنه ينجح.

إن كان اسم المستخدم غير `marwanbalushi80`، فافتح ملف `_config.yml` وغيّر سطر `url`،
وافتح `admin/config.yml` وغيّر سطر `repo`.

## ثانياً — الوسائط (اختياري، متى شئت)

الموقع يعمل الآن بصور من مصدرها الأصلي. ولجعلها ملكك بالكامل:

1. أنشئ حساباً على **cloudflare.com**، وادخل قسم **R2**، وأنشئ Bucket باسم `media`.
2. شغّل على جهازك:
   `python3 أدوات/رفع-الوسائط.py /مسار/مجلد/tweets_media`
   ينتج مجلد `للرفع` فيه كل شيء مضغوطاً.
3. ارفع محتوى المجلد إلى الـ Bucket، وفعّل **Public access** واحفظ الرابط الناتج.
4. افتح `_config.yml` وضع الرابط في `media_base`. مثال:
   `media_base: "https://pub-xxxx.r2.dev"`
5. احفظ. سيتحوّل الموقع كلّه إلى وسائطك خلال دقيقتين، **وتعمل المقاطع بالتشغيل المباشر**.

## ثالثاً — الكتابة الجديدة

افتح `https://اسم-المستخدم.github.io/admin/` وسجّل الدخول بحساب GitHub.
اضغط **New مدخل**، واختر الشكل (وقفة أو شذرة) والباب، واكتب، ثم **Publish**.
تظهر التدوينة على الموقع خلال دقيقتين.

## ما في هذا المجلد

| | |
|---|---|
| `_posts/` | ١٬٩٨٠ مدخلاً من أرشيفك |
| `assets/data.json` | فهرس المداخل الذي تقرأ منه الصفحة الرئيسة |
| `assets/radud.json` | ٤٬٠٠١ ردّ، في أرشيف مخفيّ على `/radud/` |
| `admin/` | لوحة التحرير |
| `_config.yml` | إعدادات الموقع ومصدر الوسائط |
| `أدوات/` | سكربت تجهيز الوسائط |

## تنبيهات

- الأرشيف المخفيّ على `/radud/` غير مفهرس في محرّكات البحث، لكنه ليس محمياً بكلمة سر.
  من يعرف الرابط يفتحه. فإن أردت إخفاءه تماماً، احذف `radud.html` و`assets/radud.json`.
- ٨٧٦ مدخلاً بلا باب، ولا يظهر عليها شيء أمام القارئ. صنّفها متى شئت من لوحة التحرير.
- احتفظ دائماً بنسخة من مجلد الأرشيف الأصلي على قرص خارجي.
